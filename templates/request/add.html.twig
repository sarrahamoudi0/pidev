{% extends 'base.html.twig' %}
{% block title %}Add Request {% endblock %}
{% form_theme formRequest 'bootstrap_5_layout.html.twig' %}

{% block body %}



    <div class="inner-banner">
    </div>
    <div class="w3-services py-5">
        <div class="container py-lg-4">
            {{ form_start(formRequest,{'attr': {'novalidate': 'novalidate'}}) }}
            {{ form_widget(formRequest) }}
            {{ form_end(formRequest) }}




        </div>
    </div>
{% endblock %}