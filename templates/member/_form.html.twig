<section class='container'>
    <div class='row'>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-lg btn-primary m-2">
            {{ button_label|default('Save') }}
        </button>
        {{ form_end(form) }}
    </div>
</section>
